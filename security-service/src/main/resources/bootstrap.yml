
spring:
  application:
    name: security-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://localhost:8086
server:
  port: 8098
  servlet:
    context-path: /api/v3