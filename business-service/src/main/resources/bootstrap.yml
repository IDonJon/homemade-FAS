
spring:
  application:
    name: business-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://localhost:8086
server:
  port: 8095
  servlet:
    context-path: /api/v1
